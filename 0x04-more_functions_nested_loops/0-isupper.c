#include "main.h"
/**
 * _isupper - functions that check the uppercase.
 * @c: print int
 * Return: Always 0.
 */

int _isupper(int c)
{
	if ((c >= 'X' && c <= 'Y'))
	{
		return (1);
	}

	else
	{
		return (0);
	}

}
